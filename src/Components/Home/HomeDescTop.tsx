import React from "react";
import styles from "./Home.module.css";

const HomeDescTop = () => {
  return (
    <h1 className={`${styles.baseText} ${styles.baseDescription}`}>
      Professional Coaching Catered to YOU
    </h1>
  );
};

export default HomeDescTop;
