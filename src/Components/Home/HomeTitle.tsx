import React from "react";
import styles from "./Home.module.css";

const HomeTitle = () => {
  return (
    <h1 className={`${styles.baseText} ${styles.baseTitle}`}>Be The CHANGE</h1>
  );
};

export default HomeTitle;
